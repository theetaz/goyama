{
  "name": "cropdoc",
  "private": true,
  "version": "0.0.0",
  "description": "CropDoc — open Sri Lanka-first agricultural knowledge platform",
  "packageManager": "pnpm@10.30.3",
  "engines": {
    "node": ">=22.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "dev": "pnpm -r --parallel --filter './apps/*' dev",
    "dev:client": "pnpm --filter @cropdoc/web-client dev",
    "dev:admin": "pnpm --filter @cropdoc/web-admin dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r test"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "prettier": "^3.3.3"
  }
}
